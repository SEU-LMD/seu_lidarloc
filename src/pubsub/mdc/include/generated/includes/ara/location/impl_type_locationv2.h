/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_LOCATION_IMPL_TYPE_LOCATIONV2_H
#define ARA_LOCATION_IMPL_TYPE_LOCATIONV2_H
#include <cfloat>
#include <cmath>
#include "ara/location/impl_type_header.h"
#include "impl_type_point.h"
#include "impl_type_uint16.h"
#include "impl_type_double.h"
#include "impl_type_quaternion.h"

namespace ara {
namespace location {
struct LocationV2 {
    ::ara::location::Header header;
    ::Point imuAngular;
    ::UInt16 locationState;
    ::Point position;
    ::Point positionSigma;
    ::Double velocity;
    ::Double velocitySigma;
    ::Point accel;
    ::Point accelSigma;
    ::Point imuAccel;
    ::Point pose;
    ::Point poseSigma;
    ::Point angular;
    ::Quaternion wheelSpeed;

    static bool IsPlane()
    {
        return false;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(header);
        fun(imuAngular);
        fun(locationState);
        fun(position);
        fun(positionSigma);
        fun(velocity);
        fun(velocitySigma);
        fun(accel);
        fun(accelSigma);
        fun(imuAccel);
        fun(pose);
        fun(poseSigma);
        fun(angular);
        fun(wheelSpeed);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(header);
        fun(imuAngular);
        fun(locationState);
        fun(position);
        fun(positionSigma);
        fun(velocity);
        fun(velocitySigma);
        fun(accel);
        fun(accelSigma);
        fun(imuAccel);
        fun(pose);
        fun(poseSigma);
        fun(angular);
        fun(wheelSpeed);
    }

    bool operator == (const ::ara::location::LocationV2& t) const
    {
        return (header == t.header) && (imuAngular == t.imuAngular) && (locationState == t.locationState) && (position == t.position) && (positionSigma == t.positionSigma) && (fabs(velocity - t.velocity) < FLT_EPSILON) && (fabs(velocitySigma - t.velocitySigma) < FLT_EPSILON) && (accel == t.accel) && (accelSigma == t.accelSigma) && (imuAccel == t.imuAccel) && (pose == t.pose) && (poseSigma == t.poseSigma) && (angular == t.angular) && (wheelSpeed == t.wheelSpeed);
    }
};
} // namespace location
} // namespace ara


#endif // ARA_LOCATION_IMPL_TYPE_LOCATIONV2_H
