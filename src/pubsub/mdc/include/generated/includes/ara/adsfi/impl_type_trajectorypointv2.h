/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_ADSFI_IMPL_TYPE_TRAJECTORYPOINTV2_H
#define ARA_ADSFI_IMPL_TYPE_TRAJECTORYPOINTV2_H
#include <cfloat>
#include <cmath>
#include "impl_type_double.h"

namespace ara {
namespace adsfi {
struct TrajectoryPointV2 {
    ::Double x;
    ::Double y;
    ::Double heading;
    ::Double speed;
    ::Double acc;
    ::Double steerAngle;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(x);
        fun(y);
        fun(heading);
        fun(speed);
        fun(acc);
        fun(steerAngle);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(x);
        fun(y);
        fun(heading);
        fun(speed);
        fun(acc);
        fun(steerAngle);
    }

    bool operator == (const ::ara::adsfi::TrajectoryPointV2& t) const
    {
        return (fabs(x - t.x) < FLT_EPSILON) && (fabs(y - t.y) < FLT_EPSILON) && (fabs(heading - t.heading) < FLT_EPSILON) && (fabs(speed - t.speed) < FLT_EPSILON) && (fabs(acc - t.acc) < FLT_EPSILON) && (fabs(steerAngle - t.steerAngle) < FLT_EPSILON);
    }
};
} // namespace adsfi
} // namespace ara


#endif // ARA_ADSFI_IMPL_TYPE_TRAJECTORYPOINTV2_H
