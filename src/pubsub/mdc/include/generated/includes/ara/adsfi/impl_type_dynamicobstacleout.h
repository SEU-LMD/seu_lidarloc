/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_ADSFI_IMPL_TYPE_DYNAMICOBSTACLEOUT_H
#define ARA_ADSFI_IMPL_TYPE_DYNAMICOBSTACLEOUT_H
#include <cfloat>
#include <cmath>
#include "impl_type_int32.h"
#include "impl_type_point.h"
#include "impl_type_float.h"
#include "impl_type_uint8_t.h"

namespace ara {
namespace adsfi {
struct DynamicObstacleOut {
    ::Int32 objectID;
    ::Point center;
    ::Point size;
    ::Float speed;
    ::Float heading;
    ::Float acceleration;
    ::Float speed_x;
    ::Float speed_y;
    ::Int32 type;
    ::Float yaw;
    ::uint8_t road_info;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(objectID);
        fun(center);
        fun(size);
        fun(speed);
        fun(heading);
        fun(acceleration);
        fun(speed_x);
        fun(speed_y);
        fun(type);
        fun(yaw);
        fun(road_info);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(objectID);
        fun(center);
        fun(size);
        fun(speed);
        fun(heading);
        fun(acceleration);
        fun(speed_x);
        fun(speed_y);
        fun(type);
        fun(yaw);
        fun(road_info);
    }

    bool operator == (const ::ara::adsfi::DynamicObstacleOut& t) const
    {
        return (objectID == t.objectID) && (center == t.center) && (size == t.size) && (fabs(speed - t.speed) < FLT_EPSILON) && (fabs(heading - t.heading) < FLT_EPSILON) && (fabs(acceleration - t.acceleration) < FLT_EPSILON) && (fabs(speed_x - t.speed_x) < FLT_EPSILON) && (fabs(speed_y - t.speed_y) < FLT_EPSILON) && (type == t.type) && (fabs(yaw - t.yaw) < FLT_EPSILON) && (road_info == t.road_info);
    }
};
} // namespace adsfi
} // namespace ara


#endif // ARA_ADSFI_IMPL_TYPE_DYNAMICOBSTACLEOUT_H
