/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_CHASSISGACA13_IMPL_TYPE_BCS10A_H
#define ARA_CHASSISGACA13_IMPL_TYPE_BCS10A_H
#include <cfloat>
#include <cmath>
#include "impl_type_uint8.h"
#include "impl_type_float.h"
#include "impl_type_uint16.h"

namespace ara {
namespace chassisgaca13 {
struct Bcs10A {
    ::UInt8 bcsRlwheelRotatedDirectionVd;
    ::UInt8 bcsRlwheelRotatedDirection;
    ::UInt8 bcsRlwheelSpdVd;
    ::Float bcsRlwheelSpd;
    ::UInt8 bcsRrwheelSpdVd;
    ::UInt8 bcsRrwheelRotatedDirection;
    ::UInt8 bcsRrwheelRotatedDirectionVd;
    ::Float bcsRrwheelSpd;
    ::UInt8 bcs10AMsgCounter;
    ::UInt16 bcs10AChecksum;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(bcsRlwheelRotatedDirectionVd);
        fun(bcsRlwheelRotatedDirection);
        fun(bcsRlwheelSpdVd);
        fun(bcsRlwheelSpd);
        fun(bcsRrwheelSpdVd);
        fun(bcsRrwheelRotatedDirection);
        fun(bcsRrwheelRotatedDirectionVd);
        fun(bcsRrwheelSpd);
        fun(bcs10AMsgCounter);
        fun(bcs10AChecksum);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(bcsRlwheelRotatedDirectionVd);
        fun(bcsRlwheelRotatedDirection);
        fun(bcsRlwheelSpdVd);
        fun(bcsRlwheelSpd);
        fun(bcsRrwheelSpdVd);
        fun(bcsRrwheelRotatedDirection);
        fun(bcsRrwheelRotatedDirectionVd);
        fun(bcsRrwheelSpd);
        fun(bcs10AMsgCounter);
        fun(bcs10AChecksum);
    }

    bool operator == (const ::ara::chassisgaca13::Bcs10A& t) const
    {
        return (bcsRlwheelRotatedDirectionVd == t.bcsRlwheelRotatedDirectionVd) && (bcsRlwheelRotatedDirection == t.bcsRlwheelRotatedDirection) && (bcsRlwheelSpdVd == t.bcsRlwheelSpdVd) && (fabs(bcsRlwheelSpd - t.bcsRlwheelSpd) < FLT_EPSILON) && (bcsRrwheelSpdVd == t.bcsRrwheelSpdVd) && (bcsRrwheelRotatedDirection == t.bcsRrwheelRotatedDirection) && (bcsRrwheelRotatedDirectionVd == t.bcsRrwheelRotatedDirectionVd) && (fabs(bcsRrwheelSpd - t.bcsRrwheelSpd) < FLT_EPSILON) && (bcs10AMsgCounter == t.bcs10AMsgCounter) && (bcs10AChecksum == t.bcs10AChecksum);
    }
};
} // namespace chassisgaca13
} // namespace ara


#endif // ARA_CHASSISGACA13_IMPL_TYPE_BCS10A_H
