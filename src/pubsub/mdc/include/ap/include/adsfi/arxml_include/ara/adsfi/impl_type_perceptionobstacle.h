/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_ADSFI_IMPL_TYPE_PERCEPTIONOBSTACLE_H
#define ARA_ADSFI_IMPL_TYPE_PERCEPTIONOBSTACLE_H
#include <cfloat>
#include <cmath>
#include "impl_type_uint32_t.h"
#include "impl_type_point.h"
#include "impl_type_double.h"
#include "impl_type_pointarray.h"
#include "impl_type_uint8_t.h"
#include "ara/adsfi/impl_type_time.h"
#include "impl_type_doublearray9.h"

namespace ara {
namespace adsfi {
struct PerceptionObstacle {
    ::uint32_t id;
    ::Point position;
    ::Double theta;
    ::Point velocity;
    bool hasVelocity;
    ::Point size;
    ::PointArray polygonPoint;
    ::Double trackingTime;
    ::uint8_t type;
    ::Double confidence;
    ::ara::adsfi::Time gpsTime;
    ::uint8_t confidenceType;
    ::PointArray drops;
    ::Point acceleration;
    ::Point anchorPoint;
    ::PointArray boundingBox;
    ::uint8_t subType;
    ::Double heightAboveGround;
    ::DoubleArray9 positionCovariance;
    ::DoubleArray9 velocityCovariance;
    ::DoubleArray9 accelerationCovariance;
    ::uint8_t lightStatus;

    static bool IsPlane()
    {
        return false;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(id);
        fun(position);
        fun(theta);
        fun(velocity);
        fun(hasVelocity);
        fun(size);
        fun(polygonPoint);
        fun(trackingTime);
        fun(type);
        fun(confidence);
        fun(gpsTime);
        fun(confidenceType);
        fun(drops);
        fun(acceleration);
        fun(anchorPoint);
        fun(boundingBox);
        fun(subType);
        fun(heightAboveGround);
        fun(positionCovariance);
        fun(velocityCovariance);
        fun(accelerationCovariance);
        fun(lightStatus);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(id);
        fun(position);
        fun(theta);
        fun(velocity);
        fun(hasVelocity);
        fun(size);
        fun(polygonPoint);
        fun(trackingTime);
        fun(type);
        fun(confidence);
        fun(gpsTime);
        fun(confidenceType);
        fun(drops);
        fun(acceleration);
        fun(anchorPoint);
        fun(boundingBox);
        fun(subType);
        fun(heightAboveGround);
        fun(positionCovariance);
        fun(velocityCovariance);
        fun(accelerationCovariance);
        fun(lightStatus);
    }

    bool operator == (const ::ara::adsfi::PerceptionObstacle& t) const
    {
        return (id == t.id) && (position == t.position) && (fabs(theta - t.theta) < FLT_EPSILON) && (velocity == t.velocity) && (hasVelocity == t.hasVelocity) && (size == t.size) && (polygonPoint == t.polygonPoint) && (fabs(trackingTime - t.trackingTime) < FLT_EPSILON) && (type == t.type) && (fabs(confidence - t.confidence) < FLT_EPSILON) && (gpsTime == t.gpsTime) && (confidenceType == t.confidenceType) && (drops == t.drops) && (acceleration == t.acceleration) && (anchorPoint == t.anchorPoint) && (boundingBox == t.boundingBox) && (subType == t.subType) && (fabs(heightAboveGround - t.heightAboveGround) < FLT_EPSILON) && (positionCovariance == t.positionCovariance) && (velocityCovariance == t.velocityCovariance) && (accelerationCovariance == t.accelerationCovariance) && (lightStatus == t.lightStatus);
    }
};
} // namespace adsfi
} // namespace ara


#endif // ARA_ADSFI_IMPL_TYPE_PERCEPTIONOBSTACLE_H
