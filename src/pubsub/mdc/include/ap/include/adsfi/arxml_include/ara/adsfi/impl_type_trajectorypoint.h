/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_ADSFI_IMPL_TYPE_TRAJECTORYPOINT_H
#define ARA_ADSFI_IMPL_TYPE_TRAJECTORYPOINT_H
#include <cfloat>
#include <cmath>
#include "ara/adsfi/impl_type_pathpoint.h"
#include "impl_type_double.h"

namespace ara {
namespace adsfi {
struct TrajectoryPoint {
    ::ara::adsfi::PathPoint pathPoint;
    ::Double speed;
    ::Double velocity;
    ::Double acceleration;
    ::Double relativeTime;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(pathPoint);
        fun(speed);
        fun(velocity);
        fun(acceleration);
        fun(relativeTime);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(pathPoint);
        fun(speed);
        fun(velocity);
        fun(acceleration);
        fun(relativeTime);
    }

    bool operator == (const ::ara::adsfi::TrajectoryPoint& t) const
    {
        return (pathPoint == t.pathPoint) && (fabs(speed - t.speed) < FLT_EPSILON) && (fabs(velocity - t.velocity) < FLT_EPSILON) && (fabs(acceleration - t.acceleration) < FLT_EPSILON) && (fabs(relativeTime - t.relativeTime) < FLT_EPSILON);
    }
};
} // namespace adsfi
} // namespace ara


#endif // ARA_ADSFI_IMPL_TYPE_TRAJECTORYPOINT_H
