/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef MDC_DEVM_IMPL_TYPE_FAULTDATA_H
#define MDC_DEVM_IMPL_TYPE_FAULTDATA_H
#include <cfloat>
#include <cmath>
#include "impl_type_uint32.h"
#include "impl_type_uint16.h"
#include "impl_type_uint8.h"
#include "mdc/devm/impl_type_timestamp.h"
#include "mdc/devm/impl_type_bufarray64.h"

namespace mdc {
namespace devm {
struct FaultData {
    ::UInt32 faultId;
    ::UInt16 faultObject;
    ::UInt8 faultSate;
    ::UInt8 faultType;
    ::UInt8 faultLevel;
    ::UInt8 faultSource;
    ::mdc::devm::TimeStamp faultTime;
    ::mdc::devm::BufArray64 faultDesc;

    static bool IsPlane()
    {
        return false;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(faultId);
        fun(faultObject);
        fun(faultSate);
        fun(faultType);
        fun(faultLevel);
        fun(faultSource);
        fun(faultTime);
        fun(faultDesc);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(faultId);
        fun(faultObject);
        fun(faultSate);
        fun(faultType);
        fun(faultLevel);
        fun(faultSource);
        fun(faultTime);
        fun(faultDesc);
    }

    bool operator == (const ::mdc::devm::FaultData& t) const
    {
        return (faultId == t.faultId) && (faultObject == t.faultObject) && (faultSate == t.faultSate) && (faultType == t.faultType) && (faultLevel == t.faultLevel) && (faultSource == t.faultSource) && (faultTime == t.faultTime) && (faultDesc == t.faultDesc);
    }
};
} // namespace devm
} // namespace mdc


#endif // MDC_DEVM_IMPL_TYPE_FAULTDATA_H
