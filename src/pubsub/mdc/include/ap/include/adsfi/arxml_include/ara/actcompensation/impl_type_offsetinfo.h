/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_ACTCOMPENSATION_IMPL_TYPE_OFFSETINFO_H
#define ARA_ACTCOMPENSATION_IMPL_TYPE_OFFSETINFO_H
#include <cfloat>
#include <cmath>
#include "impl_type_float.h"

namespace ara {
namespace actcompensation {
struct OffsetInfo {
    ::Float axOffset;
    ::Float ayOffset;
    ::Float azOffset;
    ::Float yawRateOffset;
    ::Float pitchRateOffset;
    ::Float rollRateOffset;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(axOffset);
        fun(ayOffset);
        fun(azOffset);
        fun(yawRateOffset);
        fun(pitchRateOffset);
        fun(rollRateOffset);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(axOffset);
        fun(ayOffset);
        fun(azOffset);
        fun(yawRateOffset);
        fun(pitchRateOffset);
        fun(rollRateOffset);
    }

    bool operator == (const ::ara::actcompensation::OffsetInfo& t) const
    {
        return (fabs(axOffset - t.axOffset) < FLT_EPSILON) && (fabs(ayOffset - t.ayOffset) < FLT_EPSILON) && (fabs(azOffset - t.azOffset) < FLT_EPSILON) && (fabs(yawRateOffset - t.yawRateOffset) < FLT_EPSILON) && (fabs(pitchRateOffset - t.pitchRateOffset) < FLT_EPSILON) && (fabs(rollRateOffset - t.rollRateOffset) < FLT_EPSILON);
    }
};
} // namespace actcompensation
} // namespace ara


#endif // ARA_ACTCOMPENSATION_IMPL_TYPE_OFFSETINFO_H
