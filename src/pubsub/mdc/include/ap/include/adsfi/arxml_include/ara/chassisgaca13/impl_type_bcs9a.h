/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_CHASSISGACA13_IMPL_TYPE_BCS9A_H
#define ARA_CHASSISGACA13_IMPL_TYPE_BCS9A_H
#include <cfloat>
#include <cmath>
#include "impl_type_uint8.h"
#include "impl_type_float.h"
#include "impl_type_uint16.h"

namespace ara {
namespace chassisgaca13 {
struct Bcs9A {
    ::UInt8 bcsFlwheelRotatedDirectionVd;
    ::UInt8 bcsFlwheelRotatedDirection;
    ::UInt8 bcsFlwheelSpdVd;
    ::Float bcsFlwheelSpd;
    ::UInt8 bcsFrwheelSpdVd;
    ::UInt8 bcsFrwheelRotatedDirection;
    ::UInt8 bcsFrwheelRotatedDirectionVd;
    ::Float bcsFrwheelSpd;
    ::UInt8 bcs9AMsgCounter;
    ::UInt16 bcs9AChecksum;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(bcsFlwheelRotatedDirectionVd);
        fun(bcsFlwheelRotatedDirection);
        fun(bcsFlwheelSpdVd);
        fun(bcsFlwheelSpd);
        fun(bcsFrwheelSpdVd);
        fun(bcsFrwheelRotatedDirection);
        fun(bcsFrwheelRotatedDirectionVd);
        fun(bcsFrwheelSpd);
        fun(bcs9AMsgCounter);
        fun(bcs9AChecksum);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(bcsFlwheelRotatedDirectionVd);
        fun(bcsFlwheelRotatedDirection);
        fun(bcsFlwheelSpdVd);
        fun(bcsFlwheelSpd);
        fun(bcsFrwheelSpdVd);
        fun(bcsFrwheelRotatedDirection);
        fun(bcsFrwheelRotatedDirectionVd);
        fun(bcsFrwheelSpd);
        fun(bcs9AMsgCounter);
        fun(bcs9AChecksum);
    }

    bool operator == (const ::ara::chassisgaca13::Bcs9A& t) const
    {
        return (bcsFlwheelRotatedDirectionVd == t.bcsFlwheelRotatedDirectionVd) && (bcsFlwheelRotatedDirection == t.bcsFlwheelRotatedDirection) && (bcsFlwheelSpdVd == t.bcsFlwheelSpdVd) && (fabs(bcsFlwheelSpd - t.bcsFlwheelSpd) < FLT_EPSILON) && (bcsFrwheelSpdVd == t.bcsFrwheelSpdVd) && (bcsFrwheelRotatedDirection == t.bcsFrwheelRotatedDirection) && (bcsFrwheelRotatedDirectionVd == t.bcsFrwheelRotatedDirectionVd) && (fabs(bcsFrwheelSpd - t.bcsFrwheelSpd) < FLT_EPSILON) && (bcs9AMsgCounter == t.bcs9AMsgCounter) && (bcs9AChecksum == t.bcs9AChecksum);
    }
};
} // namespace chassisgaca13
} // namespace ara


#endif // ARA_CHASSISGACA13_IMPL_TYPE_BCS9A_H
