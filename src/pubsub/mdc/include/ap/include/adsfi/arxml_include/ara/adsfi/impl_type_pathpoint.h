/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_ADSFI_IMPL_TYPE_PATHPOINT_H
#define ARA_ADSFI_IMPL_TYPE_PATHPOINT_H
#include <cfloat>
#include <cmath>
#include "impl_type_point.h"
#include "impl_type_double.h"

namespace ara {
namespace adsfi {
struct PathPoint {
    ::Point point;
    ::Double s;
    ::Double theta;
    ::Double kappa;
    ::Double dkappa;
    ::Double heading;
    ::Double ddkappa;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(point);
        fun(s);
        fun(theta);
        fun(kappa);
        fun(dkappa);
        fun(heading);
        fun(ddkappa);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(point);
        fun(s);
        fun(theta);
        fun(kappa);
        fun(dkappa);
        fun(heading);
        fun(ddkappa);
    }

    bool operator == (const ::ara::adsfi::PathPoint& t) const
    {
        return (point == t.point) && (fabs(s - t.s) < FLT_EPSILON) && (fabs(theta - t.theta) < FLT_EPSILON) && (fabs(kappa - t.kappa) < FLT_EPSILON) && (fabs(dkappa - t.dkappa) < FLT_EPSILON) && (fabs(heading - t.heading) < FLT_EPSILON) && (fabs(ddkappa - t.ddkappa) < FLT_EPSILON);
    }
};
} // namespace adsfi
} // namespace ara


#endif // ARA_ADSFI_IMPL_TYPE_PATHPOINT_H
