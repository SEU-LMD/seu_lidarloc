/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2020-2021. All rights reserved.
 * Generated by VRTF CM-Generator
 */

#ifndef ARA_TSYNC_IMPL_TYPE_TSYCNODEINFO_H
#define ARA_TSYNC_IMPL_TYPE_TSYCNODEINFO_H
#include <cfloat>
#include <cmath>
#include "ara/tsync/impl_type_nodename.h"
#include "ara/tsync/impl_type_managerplanetimesource.h"
#include "ara/tsync/impl_type_nodetimespec.h"
#include "ara/tsync/impl_type_nodetimesyncstate.h"
#include "ara/tsync/impl_type_nodeunsynctimes.h"
#include "ara/tsync/impl_type_nodetimeoffset.h"
#include "ara/tsync/impl_type_nodetimeoffsetthreshold.h"

namespace ara {
namespace tsync {
struct TsycNodeInfo {
    ::ara::tsync::NodeName nodeName;
    ::ara::tsync::ManagerPlaneTimeSource timeSource;
    ::ara::tsync::NodeTimeSpec mpTimeSpec;
    ::ara::tsync::NodeTimeSyncState mpTimeSyncState;
    ::ara::tsync::NodeTimeSyncState dpTimeSyncState;
    ::ara::tsync::NodeUnsyncTimes mpUnsyncTimes;
    ::ara::tsync::NodeUnsyncTimes dpUnsyncTimes;
    ::ara::tsync::NodeTimeOffset mpTimeOffset;
    ::ara::tsync::NodeTimeOffset dpTimeOffset;
    ::ara::tsync::NodeTimeOffsetThreshold mpTimeOffsetThreshold;
    ::ara::tsync::NodeTimeOffsetThreshold dpTimeOffsetThreshold;
    ::ara::tsync::NodeTimeSpec dpTimeSpec;

    static bool IsPlane()
    {
        return true;
    }


    using IsEnumerableTag = void;
    template<typename F>
    void enumerate(F& fun)
    {
        fun(nodeName);
        fun(timeSource);
        fun(mpTimeSpec);
        fun(mpTimeSyncState);
        fun(dpTimeSyncState);
        fun(mpUnsyncTimes);
        fun(dpUnsyncTimes);
        fun(mpTimeOffset);
        fun(dpTimeOffset);
        fun(mpTimeOffsetThreshold);
        fun(dpTimeOffsetThreshold);
        fun(dpTimeSpec);
    }

    template<typename F>
    void enumerate(F& fun) const
    {
        fun(nodeName);
        fun(timeSource);
        fun(mpTimeSpec);
        fun(mpTimeSyncState);
        fun(dpTimeSyncState);
        fun(mpUnsyncTimes);
        fun(dpUnsyncTimes);
        fun(mpTimeOffset);
        fun(dpTimeOffset);
        fun(mpTimeOffsetThreshold);
        fun(dpTimeOffsetThreshold);
        fun(dpTimeSpec);
    }

    bool operator == (const ::ara::tsync::TsycNodeInfo& t) const
    {
        return (nodeName == t.nodeName) && (timeSource == t.timeSource) && (mpTimeSpec == t.mpTimeSpec) && (mpTimeSyncState == t.mpTimeSyncState) && (dpTimeSyncState == t.dpTimeSyncState) && (mpUnsyncTimes == t.mpUnsyncTimes) && (dpUnsyncTimes == t.dpUnsyncTimes) && (mpTimeOffset == t.mpTimeOffset) && (dpTimeOffset == t.dpTimeOffset) && (mpTimeOffsetThreshold == t.mpTimeOffsetThreshold) && (dpTimeOffsetThreshold == t.dpTimeOffsetThreshold) && (dpTimeSpec == t.dpTimeSpec);
    }
};
} // namespace tsync
} // namespace ara


#endif // ARA_TSYNC_IMPL_TYPE_TSYCNODEINFO_H
